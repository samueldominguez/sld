SLD is a DCPU-16 linker for SOF format object files.
SLD stands for simple linker. It outputs binaries from SOF format
object files (generated by SAS). It takes n number of object files to
generate 1 binary file (big endian).

It is entirely written in C and released under the GPLv2.
If you want to contact me, my email is samuel.dominguez.lorenzo@gmail.com
bugs, issues, requests send them through Github.
